function Reorder({onReorder}) {
        return (
            <button type="button" className="reorder-btn" onClick={onReorder}>
                Reorder
            </button>
        );
}

export default Reorder;